<?xml version="1.0" encoding="UTF-8"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Phase 2: Delete additional blocking dependencies -->

    <!-- Additional Apex Classes blocking deletion -->
    <types>
        <members>PaymentCalcConfigSvc</members>
        <members>PaymentCalcConfigSvcTest</members>
        <members>PaymentCalcConfigException</members>
        <name>ApexClass</name>
    </types>

    <!-- Formula field on Opportunity referencing Payment_Calc_Config__mdt -->
    <types>
        <members>Opportunity.Setup_Fee__c</members>
        <name>CustomField</name>
    </types>

    <!-- Lookup fields on other objects referencing Payment_Schedule_Item__c -->
    <types>
        <members>Wired_Payment__c.Payment_Schedule_Item__c</members>
        <members>Integration_Log__c.Payment_Schedule_Item__c</members>
        <members>Payment_Fee__c.Payment_Schedule_Item__c</members>
        <name>CustomField</name>
    </types>

    <!-- FlexiPages that are still blocking -->
    <types>
        <members>Fee_Settlement_Detail_Record_Page</members>
        <members>Payment_Schedule_Item_Record_Page</members>
        <members>Opportunity_Record_Page_Three_Column</members>
        <name>FlexiPage</name>
    </types>

    <version>59.0</version>
</Package>
