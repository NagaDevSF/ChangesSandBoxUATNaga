<?xml version="1.0" encoding="UTF-8"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Phase 1: Delete blocking dependencies first -->

    <!-- Additional Apex Class blocking deletion -->
    <types>
        <members>PaymentCalcConfigSvc</members>
        <name>ApexClass</name>
    </types>

    <!-- Additional LWC blocking deletion -->
    <types>
        <members>programConfiguration</members>
        <name>LightningComponentBundle</name>
    </types>

    <!-- Formula field on Opportunity referencing Payment_Calc_Config__mdt -->
    <types>
        <members>Opportunity.Setup_Fee__c</members>
        <name>CustomField</name>
    </types>

    <!-- Lightning Pages that need to be deleted (blocking field/object deletions) -->
    <types>
        <members>Payment_Schedule_Item_Record_Page</members>
        <members>Payment_Plan_Record_Page</members>
        <members>Opportunity_Record_Page_Three_Column</members>
        <members>Payment_Calculator_Home</members>
        <members>Fee_Settlement_Detail_Record_Page</members>
        <name>FlexiPage</name>
    </types>

    <version>59.0</version>
</Package>
