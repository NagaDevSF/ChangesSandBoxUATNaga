<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Cleared_Amount__c</fullName>
    <description>Calculated cleared amount based on status. Cleared = Total_Payment__c. Other statuses = 0. Wire fees are tracked separately in Payment_Fee__c.</description>
    <formula>IF(
    ISPICKVAL(Status__c, &quot;Cleared&quot;),
    IF(ISBLANK(Total_Payment__c), 0, Total_Payment__c),
    0
)</formula>
    <label>Cleared Amount</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>
